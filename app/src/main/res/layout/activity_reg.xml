<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.RegActivity">
    <EditText
        android:id="@+id/reg_edit_name"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="300dp"
        android:hint="请输入手机号"
        />
    <EditText
        android:id="@+id/reg_edit_pass"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/reg_edit_name"
        android:layout_marginTop="10dp"
        android:hint="请输入密码"
        />
    <Button
        android:id="@+id/reg_button_next"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="注册"
        android:enabled="false"
        app:layout_constraintLeft_toLeftOf="@id/reg_edit_pass"
        app:layout_constraintTop_toBottomOf="@id/reg_edit_pass"
        app:layout_constraintRight_toRightOf="@id/reg_edit_pass"
        android:layout_marginTop="10dp"
        android:background="@drawable/button_enable"
        />
</android.support.constraint.ConstraintLayout>